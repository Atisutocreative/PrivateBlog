import React from 'react';
import { useParams } from 'react-router-dom';

function BlogPost() {
    //const { posten } = useParams();
    return(
        <section>
            <h1>Blogpost</h1>
            <p>test</p>
        </section>
    );

}

export default BlogPost;